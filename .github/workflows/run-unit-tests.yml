name: Run Unit Tests

on: [push]

jobs:
  run-unit-tests:
    name: Run Unit Test
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 6.0
          cache: true

      - name: Restore dependencies
        run: dotnet restore GympassNetCore/ApiGympass/ApiGympass.csproj

      - name: Build
        run: dotnet build GympassNetCore/ApiGympass/ApiGympass.csproj --configuration Release --no-restore

      - name: Run tests
        run: dotnet test --configuration Release --no-build
